import { StoreClusterValidators } from './store-cluster-validators';

describe('StoreClusterValidators', () => {
  it('should create an instance', () => {
    expect(new StoreClusterValidators()).toBeTruthy();
  });
});
